I want you to write Chapter 3 (Research Methodology) of my BSc thesis. Connect the `ur5-safe-rl-thesis` folder first.

Before touching anything, read in this order: `CLAUDE.md`, `logbook/00_INDEX.md`, `logbook/HANDOFF.md`, `logbook/06_writing.md`, `logbook/10_references.md` (the citation claim map), `logbook/11_writing_style.md`. These carry the project's working rules and current state, follow them exactly, they override any default behaviour.

Deadline: submission is 06 August. Check today's date and treat this as a hard constraint.

**Chapter 3 is not blank.** `Thesis_LaTeX/chapters/03_methodology.tex` already has 265 lines across seven sections (Problem formulation, Simulation environment and task, State/action/reward, The safety cost function, Constrained policy optimisation, Constraint calibration and cost budget, Training protocol, Evaluation protocol). Read the whole file, including its own draftnote at the top, before changing anything.

What actually needs doing, per that draftnote and the claim map:

1. **Section 3.2 (software framework) is largely absent.** It needs the `ur5_grasp/` package architecture, the cost computer, the Lagrangian runner, the training and evaluation pipeline, and the gradient-clip audit and its fix. That audit story is currently squeezed into the Results chapter's validity section and deserves its own room here instead. Find the audit details in `run_log.md` (search "gradient-clip" and "Day 23") and `logbook/09_comparison_test.md`.
2. **Check whether the chapter narrates the Day-23 gradient-clip audit withdrawal at all.** `Thesis_LaTeX/chapters/04_results.tex`'s validity section assumes Chapter 3 already sets this up. If it doesn't, add it.
3. **Missing citations**, per the claim map's Chapter 3 rows in `logbook/10_references.md`: `schulman2017ppo` (PPO baseline), `ray2019benchmarking` + `achiam2017cpo` (PPO-Lagrangian, resolved TODO-B, never cite `khan2026rl_precision_grasping` as the origin), `altman1999cmdp` (the CMDP tuple, first sentence of the formulation), `makoviychuk2021isaacgym` (simulation platform, justifies 4096 parallel envs), `mittal2023orbit` (Isaac Lab's citable predecessor), `rudin2022walk` (rsl_rl, also cite again wherever the gradient-clip audit is described), `elguea2023review` (reward/cost design as conventional practice). Check `grep -o "cite{[a-zA-Z0-9_,]*}" chapters/03_methodology.tex` first, since these may already be partially added since this prompt was written. `yoshikawa1985manipulability` is already correctly cited at the cost-function definition, don't touch that.
4. **One correction to the claim map:** its Chapter 3 row for `henderson2018matters` says "cite where n = 10 is set." That is stale. The current locked scope is **5 seeds** (1, 3, 4, 52, 54), not 10. Cite `henderson2018matters` to justify seed-dispersion reporting generally, but state 5 seeds, matching the rest of the book. Read `CLAUDE.md`'s "Results scope" section for the full lock (5 seeds, 3 arms: `ctrl`/`cppo`/`cppo15`, `ctrl` labelled "PPO (baseline)" in prose, all numbers from `Comparison_test/final_results/` only).
5. **8 em dashes need cleaning up** (`grep -c -- '---' chapters/03_methodology.tex`).

Formatting already settled, don't relitigate: 12pt Times New Roman, `book` class (not `extbook`), 1.5 line spacing via `\onehalfspacing`. `thesis-format.sty` implements all of this already.

**Writing rule, mandatory, no exceptions:** every piece of new thesis prose goes through the `humanizer` skill, draft to audit to final loop, before it's written to the file. This is a technical thesis, so the skill's PERSONALITY AND SOUL section does not apply, plain neutral prose is the correct voice here. Cut every em dash (`---` in LaTeX) as a hard rule; keep `--` only in genuine numeric ranges (`pp. 483--498`). Verify with `grep -c -- '---' chapters/03_methodology.tex` before considering the chapter done, it must return 0.

**If you add any figures:** use the `kuetfigure` environment from `thesis-format.sty`. It now takes an optional placement argument, `\begin{kuetfigure}[H]{caption}{label}`. Default to `[H]` (exact position, the `float` package, already loaded) unless you have a specific reason to float, since a floating placement like `[b]` or `[!b]` can silently drift the figure past the paragraph it belongs to and onto a much later page if the image plus caption is taller than LaTeX's default bottom-fraction limit, this happened once already in Chapter 1 and took two fix attempts to track down.

**Citation discipline:** don't cite anything that isn't in `logbook/10_references.md`'s claim map. If a genuinely new claim needs a source, add a row to the claim map first, then write the sentence, and flag that you did this. All 22 entries in `references.bib` are already verified against publisher records, don't add unverified sources.

**Scope discipline:** touch only `chapters/03_methodology.tex` (and `references.bib` / `logbook/10_references.md` if you add the missing citations). Don't edit Chapters 1, 2, 4, 5, or 6, and don't touch `Comparison_test/` data.

When done: rebuild with `latexmk -pdf main.tex` inside `Thesis_LaTeX/`, confirm exit 0 and 0 undefined citations/references, and actually render a few pages to look at rather than trusting the exit code alone. Update `run_log.md` with a dated entry and `logbook/06_writing.md`'s chapter status. If anything is genuinely ambiguous, ask me rather than guessing, the same working principles from `CLAUDE.md` apply: state assumptions, restate the goal as a concrete success criterion before executing, and don't silently fix unrelated issues you notice, just flag them.
