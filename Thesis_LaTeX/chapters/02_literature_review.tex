\chapter{Literature Review}\label{ch:litreview}

% =====================================================================
% Local formatting helpers for this chapter only. Kept here rather than in
% thesis-format.sty so the chapter can be moved or reverted on its own.
%
% 2026-08-03: the coloured/framed versions of these were removed on
% Touhid's instruction. No rules, no background tint, no colour. The
% key-findings block is plain black text separated by vertical space.
% =====================================================================
\newenvironment{keybox}{\par\addvspace{8pt}}{\par\addvspace{8pt}}

\begin{draftnote}
MODULE 3: fourth draft, 2026-08-03 (Day 26).

\textbf{History.} First drafted 2026-08-02 from metadata. Rewritten the next day against the
twelve source PDFs in \texttt{Thesis\_LaTeX/source\_papers/}, cut from 23 pages to 18, then
reconciled against the rewritten Chapter~\ref{ch:methods}. All colour and framing removed;
figure captions shortened.

\textbf{Reconciliation with Chapter~\ref{ch:methods}, 2026-08-03.} Five corrections were made
after Chapter~\ref{ch:methods} was expanded. (i) Figure~\ref{fig:b-arms} previously showed the
arms as PPO / ctrl / cPPO, which contradicts Table~3.11: the three trained arms are
\texttt{ctrl}, \texttt{cppo} at \(d=25\) and \texttt{cppo15} at \(d=15\), with the plain
\texttt{ppo} arm trained but not reported separately. The figure now matches.
(ii) \texttt{cppo15} and the budget-sensitivity question it answers were absent from this
chapter and are now a third gap. (iii) Section~\ref{sec:b-manip} no longer calls manipulability
\emph{the} operative constraint, because Section~\ref{sec:m-calib} measures joint-limit
proximity at about 86\,\% of realised cost against manipulability at 14\,\%.
\textbf{Chapter~\ref{ch:methods} \S3.8 still says ``the operative constraint of this thesis''
of the singularity term, which contradicts its own Table~3.9 and should be hedged the same
way.} (iv) The CMDP tuple and the PPO clip were stated in both chapters; this chapter now cites
Equations~\eqref{eq:cmdp} and \eqref{eq:clip} instead. The division of labour is that this
chapter carries the equations of the \emph{reviewed literature} and Chapter~\ref{ch:methods}
carries those of \emph{this thesis's method}. (v) Minor: the claim that both UR5 studies use
PPO was wrong, since Xia uses SAC alone.

\textbf{Claim discipline.} All twelve PDFs were read for the numbers quoted here, and the
reading-status table in \texttt{logbook/10\_references.md} was updated to match. Numerical
results are attributed only to papers held in full. \cite{garcia2015survey},
\cite{brunke2022safe}, \cite{elguea2023review}, \cite{ji2023safetygymnasium},
\cite{altman1999cmdp} and \cite{khan2025csrt_ibvs} are cited for framing and positioning only.

\textbf{Seed count corrected.} The first draft carried pre-scope-lock ``ten independent seeds''
language in two places. Both now read five seeds (1, 3, 4, 52, 54) per the locked scope in
\texttt{CLAUDE.md}. \textbf{Chapter~\ref{ch:results} still says ten and has not been
re-derived.} That is a live inconsistency, not one this chapter introduced.

\textbf{Figure permissions.} Figures~\ref{fig:b-ferreira} and \ref{fig:b-shen} are CC~BY.
Figures~\ref{fig:b-stooke} and \ref{fig:b-henderson} are reproduced from author preprints with
credit. Figure~\ref{fig:b-arms} is original, generated by
\texttt{Thesis\_LaTeX/tools/make\_ch2\_design\_fig.py}. Confirm the reproduction statements with
the supervisor before the final build.
\end{draftnote}

\section{How to read this chapter}\label{sec:b-guide}

This chapter makes one argument: that a safety requirement on a learned manipulation policy is
better expressed as a constraint with a stated budget than as a penalty term inside the reward,
and that the published work closest to this thesis leaves two questions about that claim
unanswered. Everything here supports, qualifies or positions that argument.

It proceeds in four movements. Sections~\ref{sec:b-motivation} to \ref{sec:b-cpo} build the
machinery: why reward shaping is an awkward vehicle for safety, what a constrained Markov
decision process is, and which deep constrained algorithm this thesis uses.
Sections~\ref{sec:b-roboticsafety} to \ref{sec:b-methods} turn to robotics and set the reviewed
studies side by side so their methods can be compared directly.
Sections~\ref{sec:b-manip} and \ref{sec:b-repro} treat the two quantities on which the work
turns: the manipulability measure that defines its safety constraint, and the seed-to-seed
variance that governs how its results must be reported. Sections~\ref{sec:b-gap} and
\ref{sec:b-summary} state the gap and close. A reader in a hurry can go straight to
Section~\ref{sec:b-gap}, which is what Chapter~\ref{ch:intro} refers back to and what
Chapter~\ref{ch:results} answers.

\begin{kuettable}{Section map for this chapter.}{tab:b-map}
\begin{tabular}{p{1.0cm}p{5.0cm}p{6.0cm}}
\hline
\textbf{\S} & \textbf{Subject} & \textbf{Principal sources} \\
\hline
2.2 & Why safety resists reward shaping & \cite{achiam2017cpo}, \cite{ray2019benchmarking} \\
2.3 & Safe RL as a field, and its taxonomy & \cite{garcia2015survey}, \cite{gu2024review} \\
2.4 & The constrained Markov decision process & \cite{altman1999cmdp} \\
2.5 & Deep constrained policy optimisation & \cite{schulman2017ppo}, \cite{achiam2017cpo},
\cite{ray2019benchmarking}, \cite{stooke2020pid} \\
2.6 & Safety in robot learning & \cite{brunke2022safe}, \cite{elguea2023review} \\
2.7 & Deep RL on manipulators and on the UR5 & \cite{shahid2022continuous_grasping},
\cite{ferreira2025grasping}, \cite{xia2024proactive} \\
2.8 & Experimental methods of the reviewed studies & all nine experimental papers \\
2.9 & Manipulability and singularity avoidance & \cite{yoshikawa1985manipulability},
\cite{shen2022reactive} \\
2.10 & Reproducibility and seed variance & \cite{henderson2018matters} \\
2.11 & Research gap and positioning & \cite{khan2026rl_precision_grasping} \\
\hline
\end{tabular}
\end{kuettable}

The findings the chapter reaches are collected below, so that the sections that follow read as
evidence for them instead of as a survey in search of a conclusion. Each is picked up again in
Section~\ref{sec:b-summary}.

\begin{keybox}
\textbf{Key findings of this chapter}

\begin{enumerate}\tightlist
\item A penalty weight and a cost budget are not two spellings of the same instruction. The
weight fixes an exchange rate between safety and task progress that the designer must guess in
advance; the budget states a limit in the units of the hazard and can be audited after training
\cite{achiam2017cpo}.
\item The constrained Markov decision process is the prevailing formalism for this
\cite{gu2024review}, and its Lagrangian relaxation turns the guessed weight into a dual variable
the optimiser sets from the observed violation \cite{altman1999cmdp}.
\item PPO-Lagrangian is preferred to CPO on published evidence. On the eighteen-environment
Safety Gym slate, CPO left a normalised violation of 0.593 against 0.026 for PPO-Lagrangian
\cite{ray2019benchmarking}.
\item The Lagrange multiplier under plain dual ascent behaves as an integral controller and
oscillates against the cost with a quarter-cycle lag \cite{stooke2020pid}. Any multiplier
reported in Chapter~\ref{ch:results} is a controller mid-flight, not a converged constant.
\item Yoshikawa's measure \(w=\sqrt{\det(JJ^{\top})}\) is the volume of the manipulability
ellipsoid and the product of the Jacobian's singular values \cite{yoshikawa1985manipulability}.
Prior learning work puts it in the reward with a hand-set weight, in one case
\(\lambda_{3}=0.05\) \cite{shen2022reactive}. This thesis puts it in a constraint.
\item Random seed alone can produce statistically distinct outcome distributions for one
algorithm on one task, at \(t=-9.0916\), \(p=0.0016\) \cite{henderson2018matters}. Means over
three seeds, which is common practice here, are weak evidence for a safety claim.
\item The closest published comparison of PPO against its constrained variant on a manipulator
\cite{khan2026rl_precision_grasping} reports no control arm isolating the cost critic from the
constraint, no seed-to-seed dispersion, and no result at more than one budget. These are the
three gaps this thesis fills.
\end{enumerate}
\end{keybox}

\section{The safety requirement in learned manipulation}\label{sec:b-motivation}

A robot manipulator that learns its own control policy poses a problem that one running a
hand-written controller does not. A classical controller can be inspected, bounded and argued
about before the machine is switched on. A policy obtained by reinforcement learning is the
outcome of an optimisation run, and is known only through the statistics of the trajectories it
produces. When such a policy is to be deployed on a physical arm sharing its workspace with
people, equipment or the workpiece, what it does in the configurations it visits only rarely is
not a secondary concern. It is the condition on which deployment depends at all.

The difficulty is that reinforcement learning offers a single channel through which a designer
expresses what the agent should and should not do: the reward function. Safety requirements
entered through that channel become penalty terms, and a penalty term needs a weight. That
weight is a trade-off rate between task performance and safety, fixed before the designer knows
what either will be worth, and it has no natural units. Achiam \emph{et al.}
\cite{achiam2017cpo} make this argument directly in motivating constrained policy optimisation:
for systems interacting physically with or around humans, it is more natural to specify a reward
function \emph{and} a set of constraints than to encode both into one scalar objective. Ray
\emph{et al.} \cite{ray2019benchmarking} put it operationally. Too small a penalty and the agent
learns unsafe behaviour; too severe a penalty and it may fail to learn at all. A fixed trade-off
also cannot respond to the fact that the pressure the reward exerts on the cost changes as the
policy improves.

Keeping the requirement outside the reward and imposing it as a constraint with a budget avoids
this. A budget can be stated in advance, in the units of the quantity being constrained, and
audited afterwards against what the trained policy actually spent. That distinction, that
\emph{a shaped reward requires the designer to guess a weight while a constraint requires only
that they state a limit}, is the premise of this thesis. Section~\ref{sec:b-manip} shows it
separating two otherwise similar bodies of work on singularity avoidance, where the guessed
weight in the nearest such study is a bare 0.05 with no stated derivation.

\section{Safe reinforcement learning}\label{sec:b-saferl}

Safe reinforcement learning studies methods that optimise a policy while respecting some notion
of acceptable behaviour during learning, at deployment, or both. The organising survey is that
of García and Fernández \cite{garcia2015survey}, which splits the literature by where the safety
requirement is inserted. One branch modifies the \emph{optimisation criterion}, changing the
objective the agent maximises through a risk-sensitive transformation, a worst-case criterion,
or explicit constraints. The other modifies the \emph{exploration process}, leaving the
objective intact but restricting the actions the agent may try, typically using external
knowledge such as a teacher, a demonstration set or a backup controller.

\begin{kuettable}{The two branches of safe reinforcement learning, after
\cite{garcia2015survey}.}{tab:b-taxonomy}
\begin{tabular}{p{3.1cm}p{4.7cm}p{4.4cm}}
\hline
\textbf{Branch} & \textbf{Mechanism} & \textbf{Evaluated on} \\
\hline
Modify the optimisation criterion & Risk-sensitive or worst-case objectives; explicit constraints
on expected cumulative cost & The trained policy, on held-out episodes \\
Modify the exploration process & Teacher guidance, demonstrations, backup controllers, action
shielding & Violations accumulated during training \\
\hline
\end{tabular}
\end{kuettable}

This thesis belongs to the first branch, and to its constrained-criterion sub-family. The
distinction matters for reading Chapter~\ref{ch:results}: second-branch methods aim to make the
learning process itself safe and are evaluated on violations accumulated \emph{during} training,
whereas first-branch methods aim to produce a policy that satisfies a requirement once trained,
and are properly evaluated on the frozen policy. This work follows the latter convention, and
Section~\ref{sec:r-design} explains why an earlier version of these results, which read safety
figures from training-time telemetry, was withdrawn. The more recent review by Gu \emph{et al.}
\cite{gu2024review} organises the post-deep-learning field around five open problems, of which
the benchmark question is why Chapter~\ref{ch:results} reports episodic cost against a declared
budget rather than an ad hoc safety score. The same review reports that the CMDP has become the
prevailing formalism for safe reinforcement learning, which places this work inside the
mainstream of the field and is why the CMDP is adopted here without further argument.

\section{The constrained Markov decision process}\label{sec:b-cmdp}

The formal object underlying that branch is the constrained Markov decision process, developed
in its modern form by Altman \cite{altman1999cmdp}. A standard Markov decision process is the
tuple \((\mathcal{S},\mathcal{A},P,r,\gamma)\) over a state space, an action space, a transition
kernel, a reward function and a discount factor. A CMDP augments this with a cost function that
scores each transition for hazard exactly as the reward scores it for progress, and a budget,
and it asks for the policy maximising expected return subject to expected accumulated cost
staying within that budget. Chapter~\ref{ch:methods} states the problem formally at
Equation~\eqref{eq:cmdp}, in the notation of the implementation, and it is not repeated here.
That division holds throughout: this chapter carries the equations of the work being reviewed,
and Chapter~\ref{ch:methods} carries those of the method being built.

Two properties matter for the argument. The safety requirement is expressed in the units of the
cost rather than of reward, so it can be set from a measurement of the system instead of a
tuning exercise, which is what the calibration of Section~\ref{sec:m-calib} depends on. And the
constrained problem admits a Lagrangian relaxation, folding the constraint into the objective
through a multiplier that is itself optimised:

\begin{equation}\label{eq:b-lagrangian}
\max_{\theta}\ \min_{\lambda\ge 0}\ \mathcal{L}(\theta,\lambda)
= J_{r}(\pi_{\theta}) - \lambda\bigl(J_{c}(\pi_{\theta}) - d\bigr).
\end{equation}

The duality theory licensing that treatment is the contribution of \cite{altman1999cmdp}, and it
is what makes the algorithms of the next section possible: instead of solving a constrained
problem directly, they solve a sequence of unconstrained ones in which the penalty weight is
driven by the observed violation. That point is easily lost, because a Lagrangian method does
end up optimising a weighted sum of reward and cost, which superficially resembles reward
shaping. The difference is that the weight is not a hyperparameter. It is a dual variable that
rises while the budget is exceeded and falls once it is met. A useful test is to ask what the
designer would change if the hazard became twice as costly to the operator. Under reward shaping
they would retune a weight and retrain until the behaviour looked right; under a constraint they
would halve the budget and let the optimiser find the weight.

\section{Constrained policy optimisation in deep reinforcement learning}\label{sec:b-cpo}

The baseline algorithm throughout this thesis is proximal policy optimisation
\cite{schulman2017ppo}, which limits the size of each policy update by clipping the probability
ratio between the candidate policy and the one that collected the data, so that a large policy
change can never be rewarded. Chapter~\ref{ch:methods} gives the clipped surrogate objective at
Equation~\eqref{eq:clip} and explains why the clip does the work of a trust region without a
constrained optimisation. What matters here is the consequence: PPO attains much of the
reliability of trust-region methods at a fraction of their implementation complexity, which is
why it is also the algorithm most commonly extended when a constraint is added. Both grasping
studies of Section~\ref{sec:b-manipulation} use it, at \(\epsilon=0.2\) in the UR5 case
\cite{ferreira2025grasping}, and so does the constrained work nearest this thesis
\cite{khan2026rl_precision_grasping}.

Constrained policy optimisation (CPO) \cite{achiam2017cpo} first brought the CMDP into deep
reinforcement learning at scale, replacing the unconstrained trust-region update with

\begin{equation}\label{eq:b-cpo}
\pi_{k+1}=\arg\max_{\pi\in\Pi_{\theta}} J_{r}(\pi)
\quad\text{s.t.}\quad J_{c}(\pi)\le d,\quad D(\pi,\pi_{k})\le\delta,
\end{equation}

solved approximately at every step so that each update both improves the return and maintains
near-constraint satisfaction. Its significance here is as much conceptual as algorithmic: it
established the constrained formulation as a practical alternative to reward shaping for
high-dimensional continuous control, and it supplies the argument quoted in
Section~\ref{sec:b-motivation}.

The algorithm actually implemented is the Lagrangian variant of PPO, as specified in the Safety
Gym benchmark report of Ray \emph{et al.} \cite{ray2019benchmarking}. Its constrained agents are
trained by gradient ascent on \(\theta\) and descent on \(\lambda\) in
Equation~\eqref{eq:b-lagrangian}, at a cost limit of \(d=25\) throughout, over 1000-step episodes
and three random seeds per environment. That budget of 25 is inherited by Chapter~\ref{ch:results}
rather than chosen freshly, which removes one degree of freedom from the present design. The
second reason to cite that report is empirical, and is why PPO-Lagrangian was preferred to CPO.

\begin{kuettable}{Normalised end-of-training metrics on the eighteen-environment Safety Gym
slate, three seeds per environment, from \cite{ray2019benchmarking}. Normalised against
unconstrained PPO, so 1.0 is the baseline and lower is safer.}{tab:b-safetygym}
\begin{tabular}{lccc}
\hline
\textbf{Algorithm} & \textbf{Return \(\bar{J}_{r}\)} & \textbf{Violation \(\bar{M}_{c}\)} &
\textbf{Cost rate \(\bar{\rho}_{c}\)} \\
\hline
PPO (unconstrained) & 1.000 & 1.000 & 1.000 \\
TRPO (unconstrained) & 1.094 & 1.132 & 1.004 \\
\textbf{PPO-Lagrangian} & \textbf{0.240} & \textbf{0.026} & \textbf{0.245} \\
TRPO-Lagrangian & 0.331 & 0.018 & 0.265 \\
CPO & 0.784 & 0.593 & 0.646 \\
\hline
\end{tabular}
\end{kuettable}

CPO retains far more return than either Lagrangian method, but leaves a normalised violation of
0.593, removing only about forty percent of the unconstrained agent's violations where the
Lagrangian methods remove roughly ninety-seven percent. Ray \emph{et al.} attribute the
shortfall to approximation error in CPO's analytic step, and note that the result contradicts the
one reported in \cite{achiam2017cpo} on easier environments. Adopting CPO would therefore have
required a justification this work does not have.

Lagrangian methods carry a characteristic weakness, and this thesis meets it directly in
Chapter~\ref{ch:results}. Stooke \emph{et al.} \cite{stooke2020pid} identify the cause precisely:
plain dual ascent on \(\lambda\) accumulates the running constraint violation, which is integral
control on the constraint and nothing else. An integral controller with no proportional or
derivative action overshoots, so the cost and the multiplier oscillate against one another with
a quarter-cycle lag, as Figure~\ref{fig:b-stooke} shows.

\begin{kuetfigure}{Cost \(g(x)\) and multiplier \(\lambda\) under the traditional Lagrangian
update (left) and under PID control (right). From \cite{stooke2020pid}, Fig.~1.}{fig:b-stooke}
\includegraphics[width=0.88\textwidth]{lit_stooke_pid_lagrangian.png}
\end{kuetfigure}

Their remedy treats the multiplier as a control input under full PID control,

\begin{equation}\label{eq:b-pid}
\Delta_{k}=J_{c}(\pi_{k})-d,\qquad
I_{k}=\bigl(I_{k-1}+\Delta_{k}\bigr)_{+},\qquad
\partial_{k}=\bigl(J_{c}(\pi_{k})-J_{c}(\pi_{k-1})\bigr)_{+},
\end{equation}
\begin{equation}\label{eq:b-pidlam}
\lambda_{k}=\bigl(K_{P}\Delta_{k}+K_{I}I_{k}+K_{D}\partial_{k}\bigr)_{+},
\end{equation}

where \((\cdot)_{+}\) is projection onto the non-negative reals and \(K_{P}=K_{D}=0\) recovers
the traditional method exactly. Two implementation details are inherited by this thesis. They
maintain a \emph{separate} cost-value approximator alongside the reward-value approximator
instead of folding \(r+\lambda c\) into a single critic, on the grounds that \(\lambda\) may
change quickly, and they rescale the policy gradient by \(1/(1+\lambda)\) so that a large
multiplier does not enlarge the effective step size. The first of those is why the constrained
agent here carries a network its baseline does not, which is in turn why a control arm is needed
at all; Section~\ref{sec:b-gap} returns to it. Their analysis is used in
Section~\ref{sec:r-variance} to read the multiplier values observed at the end of training, and
the PID form is identified in Chapter~\ref{ch:conclusion} as the natural extension of this work.
It is worth noting that \cite{stooke2020pid} names its PPO-based agent CPPO, the same
abbreviation this thesis uses. The benchmark suites have since been superseded: Safety Gymnasium
\cite{ji2023safetygymnasium} replaces the original environments and ships a library of safe
algorithms under a common protocol, and its reporting conventions, episodic cost against a
stated budget and violation rates measured per episode, are those adopted in
Chapter~\ref{ch:results}.

\section{Safety in robot learning}\label{sec:b-roboticsafety}

The literature reviewed so far approaches safety from the machine-learning side. Robotics
approaches it from the control side, with a vocabulary of invariant sets, barrier certificates
and stability guarantees that developed independently. Brunke \emph{et al.}
\cite{brunke2022safe} reconcile the two into a common framework spanning learning-based control
through to safe reinforcement learning. That reconciliation is needed here because this thesis
sits across the boundary: the quantities it constrains (the manipulability measure, the distance
to a joint limit, the clearance above the work surface) are control-theoretic constructs
describing the kinematic state of the arm, while the mechanism enforcing them is a cost critic
and a dual variable.

The survey also draws a distinction that limits what may be claimed here. Methods enforcing a
constraint in expectation, as a Lagrangian method does, bound \emph{expected} exposure to unsafe
configurations; they do not bound the severity of any individual excursion. Methods supplying a
safety certificate, such as a control barrier function filtering the commanded action, offer the
latter guarantee but require a model. Section~\ref{sec:r-safety} reports a counter-result of
exactly this shape, and Chapter~\ref{ch:conclusion} argues the two mechanisms address different
halves of the problem. Within manufacturing specifically, Elguea-Aguinaco \emph{et al.}
\cite{elguea2023review} review reinforcement learning for contact-rich manipulation from 2017
onward, and their survey is used in Chapter~\ref{ch:methods} to establish which design choices
made here follow established practice.

\section{Deep reinforcement learning for robotic manipulation}\label{sec:b-manipulation}

Applications of deep reinforcement learning to manipulation divide broadly by algorithm family.
Shahid \emph{et al.} \cite{shahid2022continuous_grasping} compare PPO against soft actor-critic
on a grasping task with a Franka Emika Panda in MuJoCo, splitting the task into a reaching and a
lifting phase with a separate dense reward for each and adding penalties on joint-position
proximity, joint acceleration and obstacle approach by fine-tuning rather than all at once. The
policy runs at 250\,Hz against a 500\,Hz joint controller and trains for \(10^{7}\) steps.
Evaluated on the physical Panda with no real-world training data, it succeeds ten times out of
ten on each of four objects. That study is the methodological template for an on-policy against
off-policy comparison, it demonstrates zero-shot transfer to hardware, and it is the precedent
for the off-policy arm planned for this study and ultimately not trained
(Section~\ref{sec:r-limits}).

Work specific to the UR5 is less common. Ferreira and Barbosa \cite{ferreira2025grasping} train
a UR5 with a Robotiq 2F-85 gripper to grasp randomly positioned objects in PyBullet at 240\,Hz,
comparing soft actor-critic against PPO under identical conditions with domain randomisation.
Their scene, in Figure~\ref{fig:b-ferreira}, is the closest published analogue to the environment
of this thesis in both robot and gripper.

\begin{kuetfigure}{A UR5 with a Robotiq 2F-85 gripper in PyBullet. From
\cite{ferreira2025grasping}, Fig.~1, CC~BY 4.0.}{fig:b-ferreira}
\includegraphics[width=0.44\textwidth]{lit_ferreira_ur5env.png}
\end{kuetfigure}

Their reward is shaped,

\begin{equation}\label{eq:b-ferreira}
R(s,a)=-\lambda\lVert p_{g}-p_{o}\rVert_{2}+r_{s}\,\mathbb{I}_{\mathrm{grasp}}
+r_{p}\,\mathbb{I}_{\mathrm{pose}},
\end{equation}

with \(\lambda=0.1\) weighting the gripper-to-object distance penalty, \(r_{s}=10\) for a grasp
held at least 0.5\,s and \(r_{p}=5\) for correct placement orientation. Their own ablation shows
how much rests on those hand-set numbers: removing the distance term costs ten percentage points
of success, the grasp reward twelve, the pose reward eight, and reducing the function to its two
sparse indicators eight. Both agents trained for 100{,}000 timesteps at a learning rate of
\(3\times10^{-4}\), batch size 64, two-layer 256-unit MLP. They report grasp success of 87\,\%
for SAC and 82\,\% for PPO, with post-grasp placement success of 75\,\% and 68\,\%. Those are the
closest published figures for a learned UR5 grasping policy, with one qualification that must be
carried into any comparison: their task requires a full physical grasp in which the fingers close
and the contact must be stable, whereas this thesis abstracts the grasp as a weld for the reasons
given in Section~\ref{sec:m-env}. The success rates of Chapter~\ref{ch:results} are therefore not
directly comparable to theirs and are not presented as though they were.

On the safety side, Xia \emph{et al.} \cite{xia2024proactive} apply deep reinforcement learning
to proactive obstacle avoidance for a UR5e in a human-robot collaborative assembly cell, using an
RGB-D camera and a wrist force-torque sensor with a soft actor-critic agent under a composite
reward, and report a 93\,\% success rate in avoiding dynamic obstacles while still reaching the
goal pose. Their work establishes that safe operation of this specific manipulator is an active
industrial concern and not an academic construction, which is the premise on which the practical
relevance of this thesis rests. It also illustrates the alternative design route: where this
thesis expresses safety as a budget on a kinematic cost, they express it as another term inside
a composite reward, and 93\,\% is a success rate rather than a bound on anything.

\section{Experimental methods of the reviewed studies}\label{sec:b-methods}

Describing each study in turn makes it hard to see what the field holds fixed and what it
varies. Table~\ref{tab:b-methods} therefore sets the experimental apparatus side by side. It
covers only the papers reporting their own experiments; the three surveys are excluded.

\begin{kuettable}{Experimental apparatus of the reviewed studies. A dash means the paper does not
state a seed count.}{tab:b-methods}
\footnotesize
\setlength{\tabcolsep}{3.5pt}
\newcolumntype{R}[1]{>{\raggedright\arraybackslash}p{#1}}
\begin{tabular}{R{2.35cm}R{2.15cm}R{1.6cm}R{2.05cm}R{0.95cm}R{2.8cm}}
\hline
\textbf{Study} & \textbf{Platform} & \textbf{Simulator} & \textbf{Algorithms} &
\textbf{Seeds} & \textbf{Headline result} \\
\hline
Ray \emph{et al.} \cite{ray2019benchmarking} & Point, Car, Doggo & MuJoCo / Safety Gym &
PPO, TRPO, both Lagrangian, CPO & 3 & PPO-Lag.\ violation 0.026 vs CPO 0.593 (normalised) \\
Stooke \emph{et al.} \cite{stooke2020pid} & Doggo & Safety Gym & CPPO with PID multiplier & --
& Cost oscillation damped; violations reduced \\
Henderson \emph{et al.} \cite{henderson2018matters} & MuJoCo locomotion & OpenAI Gym & TRPO,
PPO, DDPG, ACKTR & 5 (and 10) & Seed alone gives distinct distributions, \(p=0.0016\) \\
Shahid \emph{et al.} \cite{shahid2022continuous_grasping} & Franka Panda & MuJoCo & PPO, SAC &
-- & 100\,\% grasp success, zero-shot on hardware \\
Ferreira and Barbosa \cite{ferreira2025grasping} & UR5 + 2F-85 & PyBullet & PPO, SAC & -- &
SAC 87\,\% vs PPO 82\,\% grasp success \\
Xia \emph{et al.} \cite{xia2024proactive} & UR5e & Custom + ROS & SAC & -- & 93\,\% dynamic
obstacle avoidance \\
Shen \emph{et al.} \cite{shen2022reactive} & 4-DOF planar & Custom & SAC in Jacobian null space
& 5 & 96.8\,\% vs 77.4\,\% avoidance; higher mean \(w\) \\
Khan \emph{et al.} \cite{khan2026rl_precision_grasping} & Franka Panda & Safety Gym & PPO, cPPO
& -- & cPPO cost 12.32 vs PPO 17.76 (SOPM) \\
\hline
\textbf{This thesis} & \textbf{UR5e} & \textbf{Isaac Sim} & \textbf{PPO (baseline), cPPO
\(d=25\), cPPO \(d=15\)} & \textbf{5} & \textbf{Chapter~\ref{ch:results}} \\
\hline
\end{tabular}
\end{kuettable}

Three patterns in that table shape decisions made in Chapter~\ref{ch:methods}. The cost limit
\(d=25\) recurs: Ray \emph{et al.} set it as the Safety Gym reference configuration
\cite{ray2019benchmarking}, Khan \emph{et al.} inherit it unchanged along with \(\gamma=0.99\),
\(\lambda_{\mathrm{GAE}}=0.97\), a penalty learning rate of \(5\times10^{-2}\) and a penalty
initialised at 1.0 \cite{khan2026rl_precision_grasping}, and this thesis inherits it in turn. A
number reused across three studies is not thereby correct, but it does mean the budget in
Chapter~\ref{ch:results} is a convention and not a free parameter tuned to flatter the result.
Network capacity is also uniformly small, two hidden layers of 64 to 256 units across all of
them, and none of the reported outcomes appears limited by it, which is worth knowing before
attributing any difference in this thesis to architecture.

The third pattern is one of absence, and it matters most. Of the eight experimental studies,
three report a seed count and five do not. None of the three reports per-seed dispersion; they
report a mean, or a shaded band whose composition is not decomposed. Section~\ref{sec:b-repro}
takes up what follows.

\section{Manipulability and singularity avoidance}\label{sec:b-manip}

The constraint this thesis is built around is a floor on manipulability. One qualification
belongs here rather than later, because it changes how this section should be read. The cost
function of Chapter~\ref{ch:methods} scores three hazards, and when the thresholds were
calibrated against the unconstrained baseline the manipulability term turned out to account for
roughly 14\,\% of the realised episodic cost, against about 86\,\% for joint-limit proximity and
none at all for collision (Section~\ref{sec:m-calib}). Manipulability is therefore the framing
constraint and the one that motivates the design, but it is not the term that dominates the
budget in practice. This section develops the measure carefully all the same, because the
argument of the thesis rests on it and because the difference between this work and its nearest
neighbour is entirely a difference in where that quantity is placed.

\subsection{The measure and what it means}

Consider a manipulator with \(n\) joints, joint vector
\(\boldsymbol{\theta}\in\mathbb{R}^{n}\), whose task is described by \(m\le n\) manipulation
variables \(\mathbf{r}\in\mathbb{R}^{m}\). Differentiating the forward kinematics gives

\begin{equation}\label{eq:b-jacobian}
\dot{\mathbf{r}}=J(\boldsymbol{\theta})\,\dot{\boldsymbol{\theta}},
\qquad J(\boldsymbol{\theta})\in\mathbb{R}^{m\times n},
\end{equation}

with \(J\) the manipulator Jacobian. Yoshikawa \cite{yoshikawa1985manipulability} defines a
configuration \(\boldsymbol{\theta}^{*}\) to be \emph{singular} when
\(\operatorname{rank}J(\boldsymbol{\theta}^{*})<m\). At such a configuration the end-effector
cannot be moved instantaneously along at least one Cartesian direction, whatever joint velocities
are commanded. As a scalar index of distance from that condition he proposes

\begin{equation}\label{eq:b-manip}
w(\boldsymbol{\theta})=\sqrt{\det\bigl(J(\boldsymbol{\theta})J(\boldsymbol{\theta})^{\top}\bigr)} .
\end{equation}

Three properties established in \cite{yoshikawa1985manipulability} explain why this particular
combination is the right one, and each is used somewhere in this thesis. First, writing the
singular value decomposition \(J=U\Sigma V^{\top}\) with singular values
\(\sigma_{1}\ge\dots\ge\sigma_{m}\ge0\), the measure is their product,

\begin{equation}\label{eq:b-svd}
w=\sigma_{1}\sigma_{2}\cdots\sigma_{m},
\end{equation}

so it collapses as soon as any single \(\sigma_{i}\) does. It is sensitive to the loss of
\emph{one} direction of motion even when the arm remains perfectly capable in every other
direction, which is exactly the failure that matters. Second, the set of end-effector velocities
reachable from unit-norm joint velocities is an ellipsoid with principal axes
\(\sigma_{1}u_{1},\dots,\sigma_{m}u_{m}\), and \(w\) is proportional to its volume, so the index
measures how much end-effector motion a unit of joint effort buys. Third, when \(m=n\)
Equation~\eqref{eq:b-manip} reduces to \(w=\lvert\det J\rvert\); the UR5e has six joints
controlling a six-dimensional pose, so this is the form that applies here.

Yoshikawa's simplest worked example makes the quantity concrete. For a planar two-link arm with
link lengths \(\ell_{1},\ell_{2}\) and joint angles \(\theta_{1},\theta_{2}\),

\begin{equation}\label{eq:b-twolink}
w=\ell_{1}\ell_{2}\lvert\sin\theta_{2}\rvert ,
\end{equation}

maximised at \(\theta_{2}=\pm90^{\circ}\) and vanishing when the arm is fully extended or fully
folded, as Figure~\ref{fig:b-twolink} shows. He notes that a human arm, treated as a two-link
mechanism, satisfies \(\ell_{1}\approx\ell_{2}\) and is habitually used with the elbow near a
right angle, so people appear to adopt high-manipulability postures without being told to. The
intuition to keep is that a straightened arm is a weak arm: it can still push along its own
length, but it can barely move sideways, and a controller asking it to do so will demand very
large joint rates.

\begin{kuetfigure}{Manipulability of a planar two-link arm with equal links, from
Equation~\eqref{eq:b-twolink}. It falls to zero at both the folded and the extended posture and
peaks at a right angle.}{fig:b-twolink}
\includegraphics[width=0.74\textwidth]{lit_twolink_w.pdf}
\end{kuetfigure}

That consequence is why a floor on \(w\) counts as a safety constraint rather than a performance
target. Near a singularity the inverse mapping is ill-conditioned, so a small commanded Cartesian
velocity maps to a large joint velocity. On a real UR5e whose joints are capped at
\(180^{\circ}\mathrm{s}^{-1}\), that appears either as a violent motion or as a protective stop.
Yoshikawa anticipates this by normalising the Jacobian by the per-joint maximum velocities when
those differ, which for the UR5e they do not, since all six joints share the same ceiling. The
unnormalised form is therefore used directly in Chapter~\ref{ch:methods}, where the cost term is
an excursion below a floor \(w_{\min}\), and where Section~\ref{sec:m-calib} records the
recalibration of that floor from 0.045 to 0.06 after the baseline policy's natural operating
distribution was measured.

\subsection{The same measure, placed differently}

Shen \emph{et al.} \cite{shen2022reactive} address reactive obstacle avoidance for redundant
manipulators by learning self-motion in the null space of the Jacobian, so the commanded task
velocity is preserved exactly while the learned action redistributes the remaining freedom:

\begin{equation}\label{eq:b-nullspace}
\dot{q}_{N}=\bigl(I-J^{\dagger}J\bigr)\phi,\qquad \dot{q}_{D}=J^{\dagger}\dot{x}_{D},
\end{equation}

with \(\phi\) the learned action and \(J^{\dagger}\) the pseudo-inverse. Their agent is soft
actor-critic and their reward is the weighted sum

\begin{equation}\label{eq:b-shenreward}
r=\lambda_{1}r_{o}+\lambda_{2}r_{a}+\lambda_{3}r_{m},
\qquad
r_{o}=\sum_{i=1}^{n}\min\!\left(\frac{\lVert d_{i}\rVert}{d_{s}}-1,\,0\right),
\qquad
r_{m}=\sqrt{\det\bigl(JJ^{\top}\bigr)},
\end{equation}

in which \(r_{o}\) penalises obstacle proximity inside a safe distance \(d_{s}\), \(r_{a}\)
penalises null-space joint motion, and \(r_{m}\) is Yoshikawa's measure entered directly as a
reward term. Their reported weights are \(\lambda_{1}=1\), \(\lambda_{2}=0.2\) and
\(\lambda_{3}=0.05\), with a flat penalty of \(-10\) for a collision or joint-limit breach. The
method works: over 1000 randomly generated obstacle configurations it raises avoidance success
from 77.4\,\% to 96.8\,\% against a gradient-projection baseline in the two-obstacle scenario
while holding a higher mean manipulability throughout (3.72 against 3.63), and it computes the
null-space motion faster because a forward pass replaces an optimisation.
Figure~\ref{fig:b-shen} shows what that looks like in configuration space.

\begin{kuetfigure}{Manipulability over the \(q_{2}\)--\(q_{3}\) plane, with the paths taken by
the baseline and by the learned policy from a common start. From \cite{shen2022reactive},
Fig.~12, CC~BY 4.0.}{fig:b-shen}
\includegraphics[width=0.66\textwidth]{lit_shen_manipulability.png}
\end{kuetfigure}

The objective pursued there is the same as the one pursued here, and the quantity is identical
down to the square root. The mechanism is not. Placing manipulability in the reward requires a
weight relating a unit of \(w\) to a unit of task progress, and \(\lambda_{3}=0.05\) is that
weight. Nothing in the paper derives it, and nothing could, because there is no physical exchange
rate between an ellipsoid volume and a metre of end-effector travel. It is a number that produced
acceptable behaviour. Placing the same quantity in a constraint requires instead a budget, a
statement of how much cumulative proximity to singularity an episode may incur, which can be
measured from a baseline policy as Section~\ref{sec:m-calib} does and audited against the trained
one as Section~\ref{sec:r-safety} does. Both approaches seek the same behaviour from the same
measure. One obtains it by tuning a trade-off; the other by declaring a limit.

\section{Reproducibility and seed variance}\label{sec:b-repro}

A final strand of the literature is methodological instead of algorithmic, and it bears on the
central experimental finding of this work. Henderson \emph{et al.} \cite{henderson2018matters}
examine reproducibility in deep reinforcement learning and identify several sources of variation
that are not the contribution being claimed: network architecture and activation function,
reward scaling, choice of environment, the baseline codebase used, and the random seed. Their
treatment of the last is unusually direct. They run ten trials of TRPO on HalfCheetah-v1 under
one fixed hyperparameter configuration, varying nothing but the seed, split those ten
arbitrarily into two groups of five, and average each group. If seed were noise around a stable
mean the two curves would coincide. Figure~\ref{fig:b-henderson} shows that they do not. A
two-sample \(t\)-test across the training distribution gives \(t=-9.0916\) and \(p=0.0016\): two
groups drawn from the same algorithm, the same task and the same settings belong to
statistically distinct distributions.

\begin{kuetfigure}{TRPO on HalfCheetah-v1, two groups of five random seeds under identical
settings (\(t=-9.0916\), \(p=0.0016\)). From \cite{henderson2018matters},
Fig.~5.}{fig:b-henderson}
\includegraphics[width=0.80\textwidth]{lit_henderson_seed_variance.png}
\end{kuetfigure}

Their bootstrap intervals give the size of the effect. On HalfCheetah-v1 the 95\,\% interval for
DDPG runs from 3664 to 6574 about a mean of 5037, a span of more than half the mean, so two
papers reporting single runs of the same algorithm on that environment could differ by a factor
approaching two without either having done anything wrong. Their recommendation follows: report
over multiple independent seeds with a measure of dispersion, treat comparisons with
corresponding caution, and do not select the best of several runs. They decline to name a
required number of trials, but note that averaging fewer than five is common in published work
and is not defensible.

Set that against Table~\ref{tab:b-methods}. Ray \emph{et al.} use three seeds
\cite{ray2019benchmarking}; Shen \emph{et al.} use five and plot the minimum-to-maximum band
\cite{shen2022reactive}; five of the eight experimental studies reviewed here, including the one
nearest this thesis, do not state a seed count at all. That is not a criticism of any individual
paper, since the convention is field-wide, but it is an observation about what the published
safety numbers can support.

Three consequences follow, of different kinds. Procedurally, the protocol of
Chapter~\ref{ch:methods} trains every arm on five independent seeds (1, 3, 4, 52 and 54) and
reports dispersion across them rather than a single figure, justified by reference to
\cite{henderson2018matters} rather than by assertion, which is why this section sits in the
literature review and not in the methodology. Interpretively, the seed-to-seed spread reported in
Section~\ref{sec:r-variance} is an instance of a documented property of the method and is
presented there as a measured quantity and not as a surprising discovery. And there is a
consequence for what a safety claim is for. Henderson \emph{et al.} are concerned with fair
comparison between algorithms, which is a concern of authors. An integrator deploying a policy
has a different one: they do not obtain the mean over training runs they will never perform, but
the single policy they happened to train, on the single seed they happened to use. For that
reader the dispersion is not an error bar around the result. It is the result. That reframing is
what motivates the question Chapter~\ref{ch:results} actually asks, which is whether a constraint
tightens the spread of safety outcomes as well as shifting their centre.

\section{Research gap and positioning}\label{sec:b-gap}

The nearest published analogue to this study is that of Khan \emph{et al.}
\cite{khan2026rl_precision_grasping}, which compares a model-free PPO baseline against its
constrained variant for precision grasping on a Franka Panda in Safety Gym, and introduces a
tactile feedback scheme to accelerate learning under safety compliance. Their agent is a
two-layer 64-unit MLP actor-critic on an eight-dimensional observation, trained for 250 epochs of
2000 steps at the Safety Gym reference settings, including the cost limit of 25 this thesis also
uses. On single-object precision manipulation they report a final average episodic cost of 12.32
for cPPO against 17.76 for PPO, and on the harder multi-object task 29.95 against 38.80, in both
cases giving up a little reward for a substantially lower cost. It is the closest match in
method, framing and intent, and this thesis continues that line of work rather than departing
from it.

Three gaps are nonetheless visible in that line. Table~\ref{tab:b-gap} states them in the form
they take as design requirements, together with what this thesis does about each.

\begin{kuettable}{The three gaps and the design response to each.}{tab:b-gap}
\footnotesize
\setlength{\tabcolsep}{4pt}
\newcolumntype{G}[1]{>{\raggedright\arraybackslash}p{#1}}
\begin{tabular}{G{1.5cm}G{3.5cm}G{3.6cm}G{3.4cm}}
\hline
& \textbf{What the literature does} & \textbf{Why it is a problem} & \textbf{What this thesis
does} \\
\hline
\textbf{Gap 1.} No control arm & Trains cPPO against PPO and reports the difference
\cite{khan2026rl_precision_grasping} & cPPO also adds a cost-value network sharing an optimiser,
buffer and RNG stream with the policy, so the difference sums an implementation effect and a
constraint effect & Trains a control arm carrying the cost critic with \(\lambda\) pinned to
zero, and measures the implementation term directly (Section~\ref{sec:r-validity}) \\
\textbf{Gap 2.} No dispersion & Reports means; five of eight studies do not state a seed count
& A practitioner deploys one policy, not a mean over runs they will never perform
\cite{henderson2018matters} & Trains five seeds per arm (1, 3, 4, 52, 54) and reports the spread
of every quantity (Section~\ref{sec:r-variance}) \\
\textbf{Gap 3.} One budget only & Fixes \(d=25\) and never varies it
\cite{ray2019benchmarking, khan2026rl_precision_grasping} & A single budget cannot show whether
the effect is a property of the constraint or an artifact of one threshold & Trains a second
constrained arm at \(d=15\), so the effect can be read against how hard the constraint is
applied (Section~\ref{sec:m-arms}) \\
\hline
\end{tabular}
\end{kuettable}

Gap 1 is not a hypothetical concern. An earlier version of the present work reported a large
advantage for the constrained agent that was later traced to exactly this class of
implementation artifact and withdrawn in full, as Chapter~\ref{ch:results} records. Written as
an identity, the difference prior work reports splits into two terms of different kinds:

\begin{equation}\label{eq:b-decomp}
\underbrace{Q(\text{cPPO})-Q(\text{PPO})}_{\text{what is reported}}
=\underbrace{\bigl[Q(\text{ctrl})-Q(\text{PPO})\bigr]}_{\text{implementation term}}
+\underbrace{\bigl[Q(\text{cPPO})-Q(\text{ctrl})\bigr]}_{\text{constraint term}} ,
\end{equation}

where \emph{ctrl} carries the cost critic with its multiplier pinned to zero. The design that
resolves all three gaps is shown in Figure~\ref{fig:b-arms}. One detail of it is worth stating
in advance, because it is what makes Equation~\eqref{eq:b-decomp} usable rather than merely
true. A plain PPO arm was trained alongside the control arm on the same seeds, and its stored
network weights proved byte-identical to the control arm's. The implementation term is therefore
measured at exactly zero rather than estimated, the control arm stands in for plain PPO
throughout, and the arm labelled ``PPO (baseline)'' in Chapter~\ref{ch:results} is that control
arm. Section~\ref{sec:r-validity} gives the verification.

\begin{kuetfigure}{The three trained arms, and the two comparisons they
support.}{fig:b-arms}
\includegraphics[width=\textwidth]{lit_arms.pdf}
\end{kuetfigure}

Table~\ref{tab:b-position} sets that design against the two studies it most closely resembles.

\begin{kuettable}{Positioning against the two nearest published studies.}{tab:b-position}
\setlength{\tabcolsep}{4pt}
\newcolumntype{Q}[1]{>{\raggedright\arraybackslash}p{#1}}
\begin{tabular}{Q{4.7cm}Q{2.4cm}Q{2.4cm}Q{2.5cm}}
\hline
\textbf{Property} & \textbf{Khan \emph{et al.}} \cite{khan2026rl_precision_grasping} &
\textbf{Shen \emph{et al.}} \cite{shen2022reactive} & \textbf{This thesis} \\
\hline
Manipulator platform & Franka Panda & 4-DOF planar & UR5e \\
Safety expressed as & Constraint & Reward term & Constraint \\
Safety quantity & Collision cost & Manipulability \(w\) & Manipulability \(w\), joint limit,
collision \\
Constrained algorithm & PPO-Lagrangian & SAC (unconstrained) & PPO-Lagrangian \\
Cost-critic control arm & \(\times\) & \(\times\) & \(\checkmark\) \\
Seed count stated & \(\times\) & \(\checkmark\) (5) & \(\checkmark\) (5) \\
Per-seed dispersion reported & \(\times\) & \(\times\) & \(\checkmark\) \\
Budget varied & \(\times\) (\(d=25\) only) & n/a & \(\checkmark\) (\(d=25\) and 15) \\
Frozen-policy evaluation & \(\times\) & \(\checkmark\) & \(\checkmark\) \\
\hline
\end{tabular}
\end{kuettable}

The work is also positioned locally. It continues a line of manipulator research in the same
laboratory, of which the immediately preceding study \cite{khan2025csrt_ibvs} implemented
classical image-based visual servoing with CSRT tracking on a five-degree-of-freedom
manipulator. Where that work addressed the perception and servoing loop with classical methods,
the present work addresses the control policy with learned ones, and adds the safety constraint
that classical visual servoing does not itself provide.

\section{Summary}\label{sec:b-summary}

The path from the first section of this chapter to the last is short enough to retrace in one
pass, and doing so makes clear why the experiment of Chapter~\ref{ch:results} takes the form it
does.

It begins with a mismatch. Reinforcement learning gives a designer one channel through which to
say what the agent should do, and safety requirements do not fit down it comfortably. A hazard
entered into the reward becomes a penalty, a penalty needs a weight, and that weight is an
exchange rate between injury and task progress that nobody can derive
\cite{achiam2017cpo,ray2019benchmarking}. Section~\ref{sec:b-manip} produced the concrete
instance: in the closest published treatment of singularity avoidance by learning, that exchange
rate is \(\lambda_{3}=0.05\), and the paper offers no account of where it came from
\cite{shen2022reactive}. That is not a lapse on the authors' part, since there is nothing
available to derive it from.

The constrained Markov decision process resolves the mismatch by moving the requirement out of
the objective and into a separate condition with a budget \cite{altman1999cmdp}, an approach that
Gu \emph{et al.} \cite{gu2024review} report has become the prevailing formalism. Its Lagrangian
relaxation does eventually optimise a weighted sum, but the weight is now a dual variable driven
by the measured violation. Among the deep algorithms implementing this, PPO-Lagrangian was chosen
over CPO on published evidence: on the Safety Gym slate CPO removed about forty percent of the
unconstrained agent's violations where the Lagrangian methods removed about ninety-seven
\cite{ray2019benchmarking}. That choice carries a known cost, which Stooke \emph{et al.}
\cite{stooke2020pid} diagnose exactly. Plain dual ascent is integral control, integral control
overshoots, and the multiplier and the cost consequently chase one another around the budget. Any
\(\lambda\) reported in this thesis is a controller mid-flight, and the separate cost critic the
same authors recommend is what later forces the control arm into the experimental design.

Turning to robotics narrowed the field quickly. Learned grasping on manipulators is well
established and the numbers are respectable: 100\,\% grasp success with zero-shot hardware
transfer on a Franka Panda \cite{shahid2022continuous_grasping}, 87\,\% for SAC against 82\,\%
for PPO on a UR5 with a Robotiq gripper \cite{ferreira2025grasping}, and 93\,\% dynamic obstacle
avoidance on a UR5e in a human-robot cell \cite{xia2024proactive}. None of them is a bound,
however. They are success rates obtained under a shaped reward, and a success rate says nothing
about the configurations the arm passed through on the way. Brunke \emph{et al.}
\cite{brunke2022safe} supply the vocabulary joining the safety and manipulation literatures,
along with the caveat this thesis must respect: a constraint in expectation bounds average
exposure, not the severity of any one excursion. The operative quantity itself came from an older
source. Yoshikawa's measure \(w=\sqrt{\det(JJ^{\top})}\) is the product of the Jacobian's
singular values and the volume of the velocity ellipsoid
\cite{yoshikawa1985manipulability}, which is why it collapses when the arm loses a single
direction of motion instead of degrading gradually, and why a modest Cartesian command near that
condition becomes a large joint rate on a real UR5e.

The last piece was methodological, and it changed the question instead of the method. Henderson
\emph{et al.} \cite{henderson2018matters} showed that ten runs of one algorithm on one task,
differing only in seed, split into two groups of five that are statistically distinct at
\(p=0.0016\). Reading Table~\ref{tab:b-methods} in that light turned an incidental observation
into a finding: of the eight experimental studies reviewed, five do not state a seed count and
none reports per-seed values, so the published safety numbers in this area are means of unknown
composition. Since a practitioner deploys one policy and not a mean, the useful property of a
safety mechanism may be how tightly it constrains the spread of outcomes rather than where it
puts their centre.

Those threads converge on the three gaps of Section~\ref{sec:b-gap}, all visible in the study
nearest this one \cite{khan2026rl_precision_grasping}: no control arm separating the effect of
adding a cost critic from the effect of activating the constraint, no dispersion behind the
reported mean, and no result at any budget other than the inherited \(d=25\). None is a fault of
that paper, since all three are field-wide conventions, and all three are answerable with
experimental design and no new algorithm.

That is the scope this thesis takes on, and its boundaries should be stated as plainly as its
content. It proposes no new optimiser: the constrained agent is PPO-Lagrangian as published
\cite{ray2019benchmarking}, deliberately unmodified, because a modified algorithm would
reintroduce the confound the control arm exists to remove. It works in simulation, so its claims
concern a simulated UR5e and are transferable only in the sense that
\cite{shahid2022continuous_grasping} demonstrates such transfer is possible. It abstracts the
grasp as a weld, so its success rates are not comparable with the full-grasp figures of
\cite{ferreira2025grasping}. And although manipulability is the constraint the argument is built
on, Section~\ref{sec:m-calib} shows joint-limit proximity carrying most of the realised cost, so
the safety result is not a manipulability result alone.

Within those boundaries it contributes fifteen trained policies across three arms and five
seeds: a control arm whose weights are verified identical to plain PPO, so the implementation
term is measured at zero rather than assumed away; a constrained arm at the inherited budget;
and a second constrained arm at a tighter one, so the effect can be read against how hard the
constraint is applied. Every quantity is reported with its spread across seeds.
Chapter~\ref{ch:methods} sets out the method by which that is done.
